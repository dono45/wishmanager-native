# 小管家 - 儿童财商与愿望管理工具

> 帮助 3-12 岁儿童建立健康的金钱观和消费习惯的移动端应用。
> 完全离线运行，数据存储在本地 SQLite 数据库中。

---

## 一、产品设计愿景

### 1.1 背景

现代消费社会中，儿童过早接触到"即时满足"的消费模式，容易形成冲动消费的习惯。家长在孩子要买玩具时面临两难：无条件满足会纵容消费，直接拒绝又可能伤害亲子关系。

### 1.2 核心价值

**小管家**通过"7天冷静期"核心机制，帮助孩子和家长建立健康的消费决策流程：

1. **延迟满足训练** - 孩子需要经过7天思考才能购买想要的物品
2. **可视化进度** - 环形进度图、确认天数等视觉反馈让等待过程有成就感
3. **家长决策支持** - 急救包提供标准化的冲突应对方案
4. **游戏化激励** - 星星系统、成就徽章让孩子主动参与财商培养

### 1.3 设计原则

- **离线优先** - 所有数据本地存储，无需网络
- **儿童友好** - 童趣表情、暖色调、鼓励弹框、成就系统
- **家长可控** - 独立家长密码，预算设定、消费分析、紧急应对
- **渐进式教育** - 从冷静期到预算管理，逐步建立财商意识

---

## 二、技术架构

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **框架** | React Native + Expo | 跨平台，原项目React代码可最大程度复用 |
| **数据库** | Expo SQLite | 本地关系型数据库，完整ACID支持 |
| **状态管理** | Zustand | 轻量级全局状态管理 |
| **UI组件** | React Native Paper | Material Design 组件库 |
| **导航** | React Navigation | Bottom Tabs + Stack Navigator |
| **安全** | expo-secure-store + expo-crypto | 密码加密存储 |
| **图片** | expo-image-picker | 拍照/相册选择 |
| **日志** | 自定义分级日志 | 终端输出 + 文件持久化 |

---

## 三、代码结构

```
wishmanager-native/
├── App.tsx                        # 入口组件（主题 + 导航 + 模式路由）
├── app.json                       # Expo 应用配置（插件/权限/包名）
├── package.json                   # 依赖管理
├── tsconfig.json                  # TypeScript 配置
├── babel.config.js                # Babel 配置（路径别名）
├── metro.config.js                # Metro bundler 配置
├── jest.setup.js                  # 测试环境初始化（Mock Expo模块）
│
├── src/
│   ├── App.tsx                    # 废弃，实际入口在根目录
│   │
│   ├── db/                        # 数据库层
│   │   └── schema.ts              # 6张表Schema + 类型定义 + 迁移管理 + 种子数据
│   │
│   ├── services/                  # 业务逻辑层
│   │   ├── authService.ts         # 注册/登录/密码管理/预算设定
│   │   ├── wishService.ts         # 愿望CRUD + 冷静期核心逻辑
│   │   ├── budgetService.ts       # 月度预算生成/查询/历史
│   │   ├── taskService.ts         # 任务完成/星星统计
│   │   ├── achievementService.ts  # 成就进度/解锁
│   │   └── crisisService.ts       # 冲突记录管理
│   │
│   ├── stores/                    # Zustand 状态管理
│   │   ├── authStore.ts           # 认证状态（用户/登录态/登录登出）
│   │   └── appStore.ts            # 应用状态（模式/数据/加载）
│   │
│   ├── screens/                   # 页面组件
│   │   ├── AuthScreen.tsx         # 登录/注册（共享表单）
│   │   ├── KidHome.tsx            # 儿童模式首页（4Tab：愿望/储蓄罐/任务/成就）
│   │   └── ParentHome.tsx         # 家长模式首页（4Tab：预算/分析/急救包/账号）
│   │
│   ├── components/                # 共享组件
│   │   └── ModeSwitcher.tsx       # 儿童/家长模式切换（密码验证弹框）
│   │
│   ├── data/                      # 静态数据
│   │   └── achievements.ts        # 成就定义（名称/描述/图标/进度要求）
│   │
│   ├── utils/                     # 工具函数
│   │   └── security.ts            # 密码哈希(SHA256)/ID生成/日期工具
│   │
│   ├── logger/                    # 日志系统
│   │   └── index.ts               # 四级日志（debug/info/warn/error）+ 文件持久化
│   │
│   ├── hooks/                     # 自定义Hooks（预留）
│   │
│   └── __tests__/                 # 测试用例
│       ├── auth.test.ts           # 认证模块测试（9个用例）
│       └── wish.test.ts           # 愿望+预算模块测试（11个用例）
│
└── assets/                        # 静态资源
    ├── icon.png                   # 应用图标
    ├── splash.png                 # 启动屏
    └── adaptive-icon.png          # Android自适应图标
```

---

## 四、功能清单

### 4.1 认证模块

| 功能 | 说明 |
|------|------|
| 注册 | 用户名 + 登录密码(≥6位) + 家长密码(≥6位) |
| 登录 | 用户名 + 登录密码，成功后持久化会话 |
| 家长密码验证 | 进入家长模式时需验证 |
| 修改登录密码 | 需家长密码验证 |
| 修改家长密码 | 需原家长密码验证 |
| 退出登录 | 清除会话，返回登录页 |

### 4.2 儿童模式 - 愿望清单

| 功能 | 说明 |
|------|------|
| 添加愿望 | 名称 + 价格 + 拍照/相册上传 |
| 7天冷静期 | 添加后自动进入7天冷静期 |
| 每日确认 | "我还想要"按钮记录确认天数 |
| 第7天强制确认 | 最后一天不确认则自动过期 |
| 鼓励弹框 | 💪加油（确认时）/ 🌟你真棒（取消时） |
| 取消愿望 | "不想要了"按钮，变为cancelled |
| 冷静期结束处理 | 确认过→检查预算→自动购买/预算不足 |
| 状态查看 | cooling/wanted/purchased/insufficient/expired/cancelled |
| 检查状态 | 刷新按钮手动触发到期处理 |

### 4.3 儿童模式 - 储蓄罐

| 功能 | 说明 |
|------|------|
| 月份切换 | 前后月份导航 |
| 环形进度图 | 剩余预算百分比 |
| 预算明细 | 基础预算 + 上月滚存 + 总预算 + 已花 + 剩余 |
| 超支提示 | 剩余<0时红色警告 |
| 已购物品 | 当月购买物品列表（含缩略图） |

### 4.4 儿童模式 - 任务

| 功能 | 说明 |
|------|------|
| 星星总数 | 顶部展示当前星星数 |
| 每日任务 | 每天可重复完成 |
| 每周任务 | 每周一次，需家长确认 |
| 特殊任务 | 一次性任务，星星更多 |
| 完成任务 | 点击完成获得星星 |
| 星星兑换 | 特权说明（5⭐周末活动/10⭐晚睡/15⭐选晚餐） |

### 4.5 儿童模式 - 成就

| 成就 | 条件 |
|------|------|
| 冷静小达人 | 完成3次冷静期后仍想要并购买 |
| 储蓄小能手 | 单月剩余预算>30% |
| 消费小专家 | 连续3个月未超支 |
| 延迟满足大师 | 累计放弃5个预算不足物品 |
| 分享之星 | 使用星星兑换家庭特权 |
| 情绪小勇士 | 完成"商场不哭闹"特殊任务 |

### 4.6 家长模式 - 预算设定

| 功能 | 说明 |
|------|------|
| 滑块设置 | 0-1000元范围 |
| 精确输入 | 数字输入框 |
| 下月生效 | 新预算从下个月开始 |
| 规则说明 | 滚存/自动购买/超支扣除 |

### 4.7 家长模式 - 消费分析

| 功能 | 说明 |
|------|------|
| 年份切换 | 前后年份导航 |
| 年度统计 | 总愿望/冷静中/已购买/已取消 |
| 月度明细 | 每月预算进度条 + 基础/滚存 + 愿望统计 |

### 4.8 家长模式 - 急救包

| 功能 | 说明 |
|------|------|
| 呼吸计时器 | 开始/暂停/重置 |
| 4步应对 | 自我稳定→连接孩子→引入规则→转移注意 |
| 冲突记录 | 地点/策略/结果(成功/部分成功/升级) |
| 历史记录 | 按时间倒序列表 |

### 4.9 家长模式 - 账号

| 功能 | 说明 |
|------|------|
| 用户名显示 | 当前登录用户 |
| 修改登录密码 | 需家长密码验证 |
| 修改家长密码 | 需原家长密码 |
| 退出登录 | 清除所有会话数据 |

---

## 五、核心机制说明

### 5.1 冷静期状态机

```
                    (添加愿望)
                        ↓
                    [cooling] ← 7天冷静期
                        │
            ┌──────────┼──────────┐
            ↓          ↓          ↓
     (第7天未确认) (不想要了)  (第7天已确认)
            ↓          ↓          ↓
        [expired]  [cancelled] [wanted]
                               │
                    ┌──────────┼──────────┐
                    ↓          ↓          ↓
                (预算不足)   (取消)   (预算充足)
                    ↓                      ↓
            [insufficient]           [purchased]
```

### 5.2 预算滚存逻辑

```
每月1日:
  当月总预算 = 基础月预算 + max(0, 上月剩余)
  当月剩余 = 当月总预算
  当月已花 = 0

购买时:
  剩余 -= 物品价格
  已花 += 物品价格
```

### 5.3 第7天确认判断

```
coolingEndAt = 创建时间 + 7天
lastDay = coolingEndAt 的前一天

dailyConfirmations = JSON数组 ['2026-06-01', '2026-06-03', ...]

if dailyConfirmations 不包含 lastDay:
    → expired（视为放弃）
else:
    → wanted → 检查预算 → purchased / insufficient
```

---

## 六、部署方式

### 6.1 环境要求

- Node.js >= 18
- npm 或 yarn
- Android Studio（Android开发）或 Xcode（iOS开发）

### 6.2 开发环境搭建

```bash
# 1. 进入项目目录
cd wishmanager-native

# 2. 安装依赖
npm install

# 3. 启动 Expo 开发工具（Metro bundler，仅开发阶段使用）
npx expo start

# 4. 连接真机或模拟器运行
#   方式A：按 a 键直接在已连接的 Android 设备上运行
#   方式B：在 Android Studio 中打开 android/ 目录构建
npx expo run:android
```

> **注意**：`npx expo start` 仅在开发阶段用于代码打包和热更新。最终构建的 APK 完全离线运行，不依赖任何网络连接。

### 6.3 构建 APK（Android）

```bash
# 使用 EAS Build（推荐）
npm install -g eas-cli
eas login
eas build:android --profile preview

# 或本地构建（需 Android SDK）
cd android
./gradlew assembleRelease
```

### 6.4 数据存储位置

SQLite 数据库文件存储在应用私有目录中，随应用卸载而清除。

---

## 七、测试

### 7.1 运行测试

```bash
# 运行所有测试
npm test

# 运行并监听
npm run test:watch
```

### 7.2 测试用例覆盖

| 编号 | 模块 | 用例描述 |
|------|------|----------|
| TC-01 | 认证 | 正常注册应成功 |
| TC-02 | 认证 | 注册重复用户名应报错 |
| TC-03 | 认证 | 正常登录应成功 |
| TC-04 | 认证 | 错误密码登录应报错 |
| TC-05 | 认证 | 不存在的用户登录应报错 |
| TC-06 | 认证 | 正确家长密码验证应通过 |
| TC-07 | 认证 | 错误家长密码验证应失败 |
| TC-08 | 认证 | 使用正确家长密码修改登录密码应成功 |
| TC-09 | 认证 | 用错误家长密码修改登录密码应报错 |
| TC-10 | 愿望 | 添加愿望应创建冷静期记录 |
| TC-11 | 愿望 | 添加带图片的愿望应保存图片路径 |
| TC-12 | 愿望 | 每日确认应记录当天日期 |
| TC-13 | 愿望 | 同天重复确认应返回已确认状态 |
| TC-14 | 愿望 | 取消愿望应变为cancelled状态 |
| TC-15 | 愿望 | 获取所有愿望应返回按时间倒序排列 |
| TC-16 | 愿望 | 冷静期到期且第7天未确认应变为expired |
| TC-17 | 愿望 | 冷静期到期且第7天已确认应进入购买判断 |
| TC-18 | 预算 | 生成本月预算应创建记录 |
| TC-19 | 预算 | 重复生成预算应返回已有记录 |
| TC-20 | 预算 | 获取历史预算应返回按月倒序排列 |

---

## 八、日志系统

### 8.1 日志级别

| 级别 | 用途 |
|------|------|
| debug | 开发调试（详细操作记录） |
| info | 正常业务日志（登录/创建/购买等） |
| warn | 警告（重复确认、预算不足等） |
| error | 错误（数据库异常、网络错误等） |

### 8.2 日志输出

- **开发环境**: 终端输出全部级别日志
- **生产环境**: 终端只输出 info 及以上，同时写入 `logs/app.log` 文件

### 8.3 日志位置

```
文件系统私有目录/logs/app.log
```

---

## 九、版本信息

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0.0 | 2026-06-07 | 初始版本，完整功能实现，20个测试用例 |

---

## 十、技术选型说明

### 为什么用 React Native + Expo 而不是原生 Android？

1. **业务逻辑复用** - 原项目是 React + TypeScript，组件结构、状态管理逻辑可最大程度复用
2. **跨平台** - 同一套代码支持 Android 和 iOS
3. **开发效率** - Hot Reload 大幅提升开发速度
4. **原生性能** - Expo 模块（SQLite/SecureStore/ImagePicker）均为原生实现，无性能损失
5. **离线能力** - SQLite 提供完整的本地数据库能力，无需网络

### 为什么不实现云同步？

按需求约定，本项目为纯本地应用，数据不离开设备。如需未来扩展云同步，可在已有 SQLite 基础上增加服务端同步层，不影响现有架构。
